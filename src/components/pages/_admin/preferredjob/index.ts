import List from "./PreferredJobList";
import Create from "./PreferredJobCreate";
import Edit from "./PreferredJobEdit";
const resource = {
  list: List,
  create: Create,
  edit: Edit,
};

export default resource;
